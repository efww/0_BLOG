{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm quartz build -d docs",
  "outputDirectory": "public",
  "cleanUrls": true,
  "rewrites": [
    {
      "source": "/posts/:year/:month/:slug",
      "destination": "/posts/:year/:month/:slug.html"
    }
  ]
}
